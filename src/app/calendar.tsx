import { PlaceholderScreen } from '@/components/dev/PlaceholderScreen';

/**
 * Route: /calendar
 * Global travel calendar — presented as a modal from the root Stack.
 * Phase 2: Navigation shell only.
 */
export default function CalendarScreen() {
  return <PlaceholderScreen route="/calendar" title="Travel Calendar" />;
}
